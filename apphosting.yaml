# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 0
  # maxInstances: 100
  # concurrency: 80
  # cpu: 1
  # memoryMiB: 512

  # Environment variables and secrets.
env:
  - variable: PUBLIC_DATOCOMS_API_KEY
    secret: PUBLIC_DATOCOMS_API_KEY
  - variable: PUBLIC_DATOCMS_API_KEY
    secret: PUBLIC_DATOCMS_API_KEY
